import React, { FC } from 'react'
import { Select } from './Select'

export default {
  component: Select,
  title: 'Atoms/Select',
}

export const regular: FC = () => {
  return (
    <Select />
  )
}
